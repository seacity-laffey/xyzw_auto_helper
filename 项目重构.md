# 项目重构收尾记录

> 重构主体在 `243f3e1` 完成，剩余阻塞项已于 2026-07-16 修复并复验通过。本文档不再承担待办作用，可在确认后删除，由 Git 历史保留完成记录。

---

## 一、收尾结论

两个旧阻塞项均已关闭：

1. `DailyTasks` 已移除旧的 `authStore.isAuthenticated` 页面级判断，访问控制统一由路由层的 Token 模型负责。`/admin/daily-tasks` 不再经 `/login` 重定向到 `/tokens`。
2. lint 工具链已恢复：
   - ESLint 9.39.3 已声明为直接开发依赖。
   - `pnpm lint` 只执行检查，`pnpm lint:fix` 用于显式自动修复。
   - 已移除当前 Antfu 精简 import 插件不支持的 `import/order` 配置。
   - 生成的游戏运行时、静态数据和 `@ts-nocheck` 低层协议文件已明确排除。
   - 历史 error 使用 ESLint 9 的 `eslint-suppressions.json` 建立存量基线，避免一次性改写大量遗留代码；后续新增 error 仍会使 lint 失败。

当前 `pnpm lint` 返回 0，结果为 0 error、973 warning。warning 属于现有风格和未使用代码债务，不再阻塞本次重构收尾。

---

## 二、验证结果

以下命令已于 2026-07-16 基于当前工作区执行并通过：

```bash
pnpm lint
pnpm exec tsc --noEmit -p tsconfig.app.json
pnpm build
node --test test/roleTokenMetadata.test.js
node --test test/helperTaskRunner.test.js
node --test test/towerClimbLimit.test.js
git diff --check
```

测试共 18 项，全部通过。

### 生产构建浏览器验收

使用生产构建产物进行本地预览。浏览器注入仅用于页面渲染的本地 Token，并屏蔽真实外部 WebSocket 连接。

| 路由 | 结果 | 验收内容 |
| --- | --- | --- |
| `/admin/dashboard` | 通过 | 保持目标路由；页面正常渲染；独立 JS/CSS chunk 返回 200 |
| `/admin/game-features` | 通过 | 保持目标路由；页面正常渲染；独立 JS/CSS chunk 返回 200 |
| `/admin/daily-tasks` | 通过 | 保持目标路由；页面正常渲染；不再触发旧登录重定向；独立 JS/CSS chunk 返回 200 |
| `/admin/batch-daily-tasks` | 通过 | 保持目标路由；页面正常渲染；独立 JS/CSS chunk 返回 200 |

四个路由均无控制台错误、页面错误、懒加载资源 4xx/5xx 或本地资源请求失败。

批量任务页面基础交互已通过：

- 选择账号后，批量操作按钮可用。
- 可切换到“副本”标签并正常显示对应操作。

---

## 三、非阻塞技术债务

- `eslint-suppressions.json` 记录了当前遗留 error 基线。修改旧代码时可逐步修复对应问题，并使用 ESLint 的 `--prune-suppressions` 清理失效条目。
- 当前 973 条 warning 可按功能区域逐步清理，不建议为清零数字进行一次性全仓格式化。
- 不继续补无法确认真实字段的后端协议命令级类型。
- 暂不拆除低层动态协议或遗留脚本上的类型检查边界。
- 暂不做 `GameFeatures` 内部卡片级动态导入；仅在后续仍需要压缩首屏体积时再评估。
