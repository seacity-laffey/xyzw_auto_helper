# 项目重构待办

> 本文档只记录当前仍需处理或人工确认的事项。已经完成的重构、迁移和构建优化不再在此展开。

---

## 一、待处理

- 运行并修复 `npm run lint` 产出的纯前端风格问题。
- 浏览器手动验收核心路由和批量任务页面:
  - `/admin/dashboard`
  - `/admin/game-features`
  - `/admin/daily-tasks`
  - `/admin/batch-daily-tasks`
- 验收重点:
  - 路由进入正常,无重复路由或空白页。
  - 拆包后懒加载页面正常渲染。
  - 批量任务页面基础交互正常。

---

## 二、暂不处理

- 不继续补无法确认真实字段的后端协议命令级类型。
- 暂不拆除低层动态协议/遗留脚本上的类型检查边界。
- 暂不做 `GameFeatures` 内部卡片级动态导入;仅在后续仍需要压缩首屏体积时再评估。

---

## 三、当前验证命令

```bash
npx tsc --noEmit -p tsconfig.app.json
pnpm build
git diff --check
```

当前三项已验证通过。`npm run lint` 尚未执行。
