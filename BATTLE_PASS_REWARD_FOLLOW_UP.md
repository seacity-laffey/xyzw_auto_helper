# 通行证奖励后续实现说明

本文档记录批量日常任务中“领取通行证奖励”的当前状态、协议证据和后续实现要求。当前阶段仅完成分析，尚未实现领取资格判断。

## 当前行为

批量日常任务执行到“领取通行证奖励”时，会直接发送：

```text
command: activity_recyclewarorderrewardclaim
body: { actId: 1 }
```

当前代码没有先判断是否存在已解锁且未领取的通行证奖励。因此，这一项与每日任务、固定奖励等已经完成状态过滤的领取逻辑不同，仍可能在没有可领取奖励时发送请求。

## 已确认的协议数据

本次分析使用本地抓包文件：

```text
/Users/sea/Documents/xyzw-protocol-2026-09-07T13-51-53-100Z.json
```

`activity_getresp.body.activity.warOrderActivityInfo` 中包含通行证活动状态。已观察到 `actId: 1` 的以下字段：

| 字段 | 已观察到的值 | 当前理解 |
| --- | --- | --- |
| `actId` | `1` | 通行证活动 ID，与当前领取请求一致 |
| `purchased` / `purchased2` / `purchased3` | `false` | 不同付费奖励轨道的购买状态，具体轨道映射待确认 |
| `rewardClaimed` | `1..17` 的值均为 `1` | 已领取奖励记录，但键与奖励等级的精确映射待确认 |
| `newRewardClaimed` | `{}` | 另一组领取记录，适用场景待确认 |
| `complete` | `{}` | 可能是任务完成状态，尚无有效样本 |
| `taskClaimed` | `{}` | 可能是通行证任务领取状态，尚无有效样本 |
| `itemNum` | `0` | 可能参与通行证进度计算，但当前样本不足以确认 |
| `phaseTh` | `0` | 阶段阈值或阶段信息，含义待确认 |
| `unlockClaimed` | `false` | 解锁奖励领取状态，适用条件待确认 |
| `dailyTime` / `weekTime` | `0` | 日/周任务刷新或状态时间，含义待确认 |

领取命令的响应映射已观察到：

```text
activity_warorderclaimresp -> activity_recyclewarorderrewardclaim
```

现有样本中通行证奖励已领取，无法从这一份数据确定“当前进度足够且有奖励待领取”时的完整判定条件。

## 缺失证据

实现前需要一份至少包含一个可领取通行证奖励的前后对照抓包，用于确认：

1. 当前通行证进度取自 `itemNum`，还是其他活动或角色字段。
2. 奖励等级、`rewardClaimed` 键和值之间的对应关系。
3. `newRewardClaimed` 与 `rewardClaimed` 分别对应哪些奖励轨道。
4. 免费轨道和各付费轨道的解锁、领取规则。
5. `phaseTh`、`unlockClaimed` 等字段是否参与领取资格判断。
6. `{ actId: 1 }` 是领取全部可领取奖励，还是只领取某一类奖励。
7. 领取后由哪个响应或推送同步最新状态。

## 后续抓包步骤

1. 在重新登录或刷新页面前启动协议记录。
2. 打开游戏内当前通行证页面，保留首次 `activity_get` / `activity_getresp`。
3. 确保至少有一个通行证奖励已达到领取条件且尚未领取。
4. 导出领取前状态。
5. 只执行一次游戏内领取操作，记录请求体、`activity_warorderclaimresp` 及相关同步推送。
6. 重新打开或刷新通行证页面，导出领取后状态。
7. 如果存在免费与付费奖励轨道，分别保留可领取样本；没有购买付费通行证时也需要保留对应状态。

原始协议文件可能包含账号、连接或会话信息，只保存在本地分析，不应提交到仓库。

## 预期实现

后续实现应遵循与其他领取任务一致的策略：

1. 先发送只读活动状态查询，取得最新通行证状态。
2. 根据进度、奖励等级和已领取映射计算是否存在可领取奖励。
3. 仅在至少一个奖励“已解锁且未领取”时发送游戏原生领取命令。
4. 状态查询失败或字段不完整时跳过领取并记录原因，不盲目发送请求。
5. 继续使用游戏原生批量领取能力，不在辅助端逐档模拟领取，除非后续协议证据证明必须逐档请求。

## 验收与测试

实现时至少覆盖以下情况：

- 所有已解锁奖励均已领取：不发送领取请求。
- 存在一个或多个已解锁未领取奖励：只发送一次游戏原生领取请求。
- 进度不足：不发送领取请求。
- 未购买付费轨道：不得把付费奖励误判为可领取。
- 已购买不同付费轨道：按实际解锁状态判断。
- 活动状态缺失、响应超时或解析失败：跳过领取并输出可定位的日志。
- 领取完成后再次执行：不重复发送领取请求。
