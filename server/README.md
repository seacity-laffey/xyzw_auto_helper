# TokenURL Service Status

This directory only retains the historical Python dependency list. The Flask
application, templates, configuration management, and persistence code are not
present in this repository, so this directory is not a deployable service.

Do not expose the default credentials described by older documentation. A new
server implementation must define its authentication, storage, configuration,
and deployment model before this path can be restored.
