# 当前功能基线

本文档描述当前代码实际提供的功能，用于维护本仓库并与 `upstream` 比较差异。

- 功能基线分支：`refactor/dev`
- 上游同步点：`4169559`（`main`：`b66570b`）
- 核对日期：2026-09-02
- 判断依据：正式路由、可达页面及其直接使用的组件和 Store

## 正式入口

当前应用有五个正式页面入口：

| 页面     | 路由              | 账号要求 | 主要用途                                             |
| -------- | ----------------- | -------- | ---------------------------------------------------- |
| 账号管理 | `/tokens`         | 否       | 导入、维护、分组和启动游戏账号                       |
| 角色详情 | `/role`           | 是       | 通过左侧账号目录查看角色状态并使用单账号功能         |
| 批量任务 | `/batch-tasks`    | 是       | 默认工作区；使用左侧多选账号编排、执行和定时运行任务 |
| 主线推关 | `/pushing-levels` | 是       | 为多个账号执行主线自动推关                           |
| 内嵌游戏 | `/game`           | 是       | 多开内嵌游戏、选择主控窗口并同步画布操作             |

左侧账号目录在工作区内常驻：点击账号名称只聚焦并进入角色详情，不会自动上线；复选框控制批量范围，使用左侧“上线 / 下线”显式管理所选账号连接。应用路由不使用账号作为权限条件；没有账号时仍可进入工作区并查看空状态，具体操作在缺少所需账号时禁用或提示。

## 账号管理

### 导入方式

- 手动输入
- 微信扫码
- BIN 多角色
- BIN 单角色
- 兼容历史 URL 查询参数导入（不在界面提供入口）

### 管理能力

- 列表和卡片两种视图
- 按名称、服务器、创建时间或最后使用时间排序
- 编辑、删除、拖动排序和添加备注
- Token 配置导入和导出
- 临时 Token 升级为长期有效
- 只读显示每个账号的 WebSocket 连接状态
- 从角色信息补齐缺失的角色名称并同步服务器名称
- 账号管理页不会自动连接、刷新或清理账号
- 内嵌游戏入口位于角色详情页

账号元数据、当前选择和分组保存在 `localStorage`。连接需要使用的二进制 Token 数据按账号保存在 IndexedDB；两种存储共同组成当前持久化方案。

## 角色详情

单角色页面负责连接当前账号，并按以下分区提供状态查看和操作：

| 分区   | 当前内容                                                                 |
| ------ | ------------------------------------------------------------------------ |
| 日常   | 角色身份、阵容、每日任务、咸将塔、怪异塔、盐罐助手、挂机状态             |
| 俱乐部 | 俱乐部信息、签到入口、当日篝火攻打统计和成员次数导出                     |
| 活动   | 月度任务、答题活动、换皮闯关                                             |
| 工具   | 无限阵容、宝箱、钓鱼、招募、升星、竞技场、梦境、武将升级、洗练、消耗活动 |
| 盐场   | 盐场排行（两套展示样式）、周战绩、月战绩、地图和战况                     |
| 蟠桃园 | 蟠桃园信息（两套展示样式）和战绩                                         |
| 排行榜 | 区服榜、巅峰榜、俱乐部榜、黄金积分榜、伟大航路积分榜                     |
| 切磋   | 玩家切磋功能                                                             |

页面会显示当前 WebSocket 状态，支持连接和断开，并在连接后初始化角色、爬塔、怪异塔、阵容和战斗版本数据。

## 批量任务

### 任务分区

- 日常：挂机、盐罐、俱乐部签到、竞技场、珍宝阁等
- 副本：咸将塔、蟠桃园、换皮闯关及自动领奖等
- 怪异塔
- 资源：宝箱积分、招募、图鉴奖励等
- 功法
- 月度任务
- 巅峰赛竞猜

### 编排能力

- 多账号选择、全选和分组
- 立即开始、停止及执行日志
- 账号级任务设置和批量设置
- 任务模板的创建、编辑、应用和管理
- 基于 Cron 表达式的定时任务
- 定时任务启停、编辑和删除
- 任务配置与账号配置的导入、导出
- WebSocket 连接队列、并发限制和任务超时恢复
- 咸将塔和怪异塔的待领取章节奖励自动补领
- 按批量账号勾选结果在同一页面打开多个独立游戏窗口
- 多开游戏可启用操作同步、指定主控窗口，并按画布比例同步鼠标点击、拖动和单指触摸滑动
- 上号器统一放在游戏页工具栏，通过一个公共面板选择目标窗口、导入或清空 BIN

## 公共能力

- Vue 3、Pinia、Vue Router 和 Vite
- shadcn-vue 风格的仓库内 UI 组件、Tailwind CSS 和 SCSS；部分大型业务组件仍在迁移 Naive UI、Arco Design 与 UnoCSS
- 浅色、深色及跟随系统的主题模式
- BON 协议编解码和游戏 WebSocket 通信
- LX、X、XTM 等协议处理逻辑
- 内嵌 Cocos 游戏、账号会话注入、单页多账号窗口及同源画布操作同步
- Cloudflare Pages Worker 代理
- 可选的 Token URL 配套服务

仓库还包含独立的 `render-backend/` 实验服务，用于 BON 协议和游戏连接相关的后端能力。它有独立依赖与启动入口，尚未接入根项目的前端构建和正式路由。

## 兼容与遗留边界

下列旧地址仅用于兼容，会跳转到现有正式入口：

- `/admin`、`/admin/dashboard`、`/admin/batch-daily-tasks`
- `/admin/daily-tasks`、`/admin/game-features`、`/admin/profile`
- `/admin/message-test`、`/admin/legion-war`、`/admin/PushingLevels`
- `/websocket-test`、`/login`、`/register`、`/game-roles`

上述地址只用于接住浏览器书签和历史记录。仓库不再包含管理员后台、登录注册页面或对应认证 Store；`admin` 不是当前产品模块，也不存在可登录的管理员功能。

## 当前验证状态

在当前功能基线上已执行：

- `npm run build`：通过
- `./node_modules/.bin/tsc --noEmit`：通过
- `node --test test/*.test.js`：39 项通过
- `git diff --check`：通过（内嵌游戏的上游编译产物除外）
- `eslint src --ext .vue,.js,.ts --pass-on-unpruned-suppressions`：0 个 error；仍保留不阻断检查的既有 warning

旧文档中关于 lint 无遗留项、旧页面可独立访问的结论不再代表当前工作区。

## 与 upstream 的比较口径

同步上游前后应至少比较：

1. 正式路由和导航入口是否变化。
2. Token 导入、刷新和持久化方式是否变化。
3. 单角色分区及功能卡片是否增删。
4. 批量任务类型、调度、模板和导入导出是否变化。
5. WebSocket 命令、事件处理和协议类型是否变化。
6. 本仓库 UI、角色元数据同步和工程配置是否需要保留。
