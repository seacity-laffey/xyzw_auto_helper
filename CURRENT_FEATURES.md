# 当前功能基线

本文档描述当前代码实际提供的功能，用于维护本仓库并与 `upstream` 比较差异。

- 功能基线提交：`e7bcbfd`
- 核对日期：2026-08-11
- 判断依据：正式路由、可达页面及其直接使用的组件和 Store

## 正式入口

当前应用只有三个正式页面入口：

| 页面 | 路由 | Token 要求 | 主要用途 |
| --- | --- | --- | --- |
| Token 管理 | `/tokens` | 否 | 导入、维护和选择游戏账号 |
| 单个角色 | `/admin/game-features` | 是 | 查看角色状态并使用单账号功能 |
| 批量任务 | `/admin/batch-daily-tasks` | 是 | 为多个账号编排、执行和定时运行任务 |

没有 Token 时，受保护页面会跳转到 `/tokens`。有 Token 但尚未选择账号时，路由守卫会自动选择第一个账号。

## Token 管理

### 导入方式

- 手动输入
- URL 获取
- 微信扫码
- BIN 多角色
- BIN 单角色
- URL 查询参数自动导入

### 管理能力

- 列表和卡片两种视图
- 按名称、服务器、创建时间或最后使用时间排序
- 选择、编辑、删除、拖动排序和添加备注
- 单个刷新及批量刷新支持自动刷新的 Token
- Token 配置导入和导出
- 临时 Token 清理及升级为长期有效
- 显示并控制每个账号的 WebSocket 连接状态
- 从角色信息补齐缺失的角色名称并同步服务器名称

账号元数据、当前选择和分组保存在 `localStorage`。连接需要使用的二进制 Token 数据按账号保存在 IndexedDB；两种存储共同组成当前持久化方案。

## 单个角色

单角色页面负责连接当前账号，并按以下分区提供状态查看和操作：

| 分区 | 当前内容 |
| --- | --- |
| 日常 | 角色身份、阵容、每日任务、咸将塔、怪异塔、盐罐助手、挂机状态 |
| 俱乐部 | 俱乐部信息、签到入口和疯狂赛车 |
| 活动 | 月度任务、答题活动、换皮闯关 |
| 工具 | 无限阵容、宝箱、钓鱼、招募、升星、竞技场、梦境、武将升级、洗练、消耗活动、咸王宝库 |
| 盐场 | 盐场排行、周战绩、月战绩、地图和战况 |
| 蟠桃园 | 蟠桃园信息和战绩 |
| 排行榜 | 区服榜、巅峰榜、俱乐部榜、黄金积分榜、伟大航路积分榜 |
| 切磋 | 玩家切磋功能 |

页面会显示当前 WebSocket 状态，支持连接和断开，并在连接后初始化角色、爬塔、怪异塔、阵容和战斗版本数据。

## 批量任务

### 任务分区

- 日常：挂机、盐罐、俱乐部签到、竞技场、珍宝阁等
- 副本：咸将塔、蟠桃园等
- 宝库
- 怪异塔
- 资源：宝箱积分、招募、图鉴奖励等
- 功法
- 月度任务

### 编排能力

- 多账号选择、全选和分组
- 立即开始、停止及执行日志
- 账号级任务设置和批量设置
- 任务模板的创建、编辑、应用和管理
- 基于 Cron 表达式的定时任务
- 定时任务启停、编辑和删除
- 任务配置与账号配置的导入、导出
- WebSocket 连接队列、并发限制和任务超时恢复

## 公共能力

- Vue 3、Pinia、Vue Router 和 Vite
- Naive UI、Arco Design、UnoCSS、Tailwind CSS 和 SCSS
- 浅色、深色及跟随系统的主题模式
- BON 协议编解码和游戏 WebSocket 通信
- LX、X、XTM 等协议处理逻辑
- Cloudflare Pages Worker 代理
- 可选的 Token URL 配套服务

## 兼容与遗留边界

下列旧地址仅用于兼容，均会跳转到现有三个正式入口之一：

- `/admin`、`/admin/dashboard`、`/admin/daily-tasks`
- `/admin/profile`、`/admin/message-test`、`/admin/legion-war`
- `/websocket-test`、`/login`、`/register`、`/game-roles`

仓库仍保留部分旧页面及旧 Store，例如登录、注册、Dashboard、Profile、`auth.ts`、`gameRoles.ts` 和 `localTokenManager.ts`。它们不属于当前正式路由，不应仅因文件存在就视为现有产品功能。

## 当前验证状态

在功能基线提交上已执行：

- `npm run build`：通过
- `node --test test/roleTokenMetadata.test.js`：3 项通过
- `git diff --check`：通过
- `npm run lint`：未通过，共 431 个 error、960 个 warning

旧文档中关于 lint 为 0 error、旧页面可独立访问的结论不再代表当前工作区。

## 与 upstream 的比较口径

同步上游前后应至少比较：

1. 正式路由和导航入口是否变化。
2. Token 导入、刷新和持久化方式是否变化。
3. 单角色分区及功能卡片是否增删。
4. 批量任务类型、调度、模板和导入导出是否变化。
5. WebSocket 命令、事件处理和协议类型是否变化。
6. 本仓库 UI、角色元数据同步和工程配置是否需要保留。
